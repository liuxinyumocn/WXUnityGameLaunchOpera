import EventEmitter from '../utils/EventEmitter';
export default class Logger {
    static readonly eventEmitter: EventEmitter;
    private startTimestamp;
    constructor();
    private registerLoggerEventListener;
}
